# DEPRECATED: This workflow is replaced by pr_update_changelog.yml
# The new workflow updates the changelog in the PR before merge, avoiding branch protection issues
# 
# name: Post-Merge CHANGELOG Update
# 
# on:
#   push:
#     branches: [main]
#     paths-ignore:
#       - 'CHANGELOG.md'
#       - 'pyproject.toml'

# 
# concurrency:
#   group: changelog-update
#   cancel-in-progress: false
# 
# jobs:
#   update_changelog:
#     name: Update CHANGELOG and Version
#     runs-on: ubuntu-latest
#     permissions:
#       contents: write
#     
#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v4
#         with:
#           fetch-depth: 0  # Fetch all history for git log analysis
#           token: ${{ secrets.GITHUB_TOKEN }}
#           
#       - name: Set up Python
#         uses: actions/setup-python@v5
#         with:
#           python-version: '3.10'
#       
#       - name: Install dependencies
#         run: |
#           pip install 'anthropic>=0.79.0,<1.0.0' tomlkit==0.13.2
#       
#       - name: Update CHANGELOG and bump version with Claude
#         env:
#           ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
#         run: |
#           python scripts/update_changelog.py
#       
#       - name: Commit and push changes
#         run: |
#           git config --local user.email "github-actions[bot]@users.noreply.github.com"
#           git config --local user.name "github-actions[bot]"
#           
#           # Check if there are changes to commit
#           if git diff --quiet; then
#             echo "No changes to commit"
#             exit 0
#           fi
#           
#           git add pyproject.toml CHANGELOG.md
#           git commit -m "chore: update CHANGELOG.md and bump version [skip ci]"
#           
#           # Pull and retry push in case of concurrent updates
#           max_retries=3
#           retry_count=0
#           
#           while [ $retry_count -lt $max_retries ]; do
#             if git push; then
#               echo "Successfully pushed changes"
#               exit 0
#             else
#               echo "Push failed, pulling and retrying... (attempt $((retry_count + 1))/$max_retries)"
#               git pull --rebase origin main
#               retry_count=$((retry_count + 1))
#               sleep 2
#             fi
#           done
#           
#           echo "Failed to push after $max_retries attempts"
#           exit 1
